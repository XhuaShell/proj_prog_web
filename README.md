# Car dealership

## Steps to run the app

After configuring the database access details in the .env file, you will need to run the following commands to create the schema.

```bash
npx drizzle-kit generate
npx drizzle-kit migrate
```

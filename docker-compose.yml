services:
  myDB:
    image: postgres:17
    container_name: db-cocesionario
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - ${DB_PORT}:5432
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_DATABASE}
    volumes:
      - ./postgres_web_concesionario:/var/lib/postgresql/data

volumes:
  postgres_web_concesionario: